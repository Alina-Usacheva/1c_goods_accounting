
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Отбор = Новый Структура;
	Отбор.Вставить("Номенклатура", Объект.Ссылка); 
	ИсторияЦенТоваровСрезПоследних = РегистрыСведений.ИсторияЦенТоваров.СрезПоследних(ТекущаяДата(), Отбор);
	
	Если ИсторияЦенТоваровСрезПоследних.Количество() > 0 Тогда
		Цена = ИсторияЦенТоваровСрезПоследних[0].Цена;
	КонецЕсли;
	
КонецПроцедуры
